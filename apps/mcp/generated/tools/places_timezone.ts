// AUTO-GENERATED by scripts/generate.ts — do not edit by hand.
// Source operation: GET /v1/places/timezone
// Regenerate with: bun run scripts/generate.ts --input <spec> --output generated

import { z } from "zod";
import { requiemsRequest } from "../runtime";

/**
 * Returns timezone information for the given coordinates or city name. Provide either `city` or both `lat` and `lon`.
 */
export const inputSchema = z.object({
  lat: z.string().optional().describe("Latitude of the location (-90 to 90). Required when using coordinate-based lookup."),
  lon: z.string().optional().describe("Longitude of the location (-180 to 180). Required when using coordinate-based lookup."),
  city: z.string().optional().describe("City name for city-based lookup (e.g. 'Tokyo', 'London'). Required when not using coordinates."),
});

export type PlacesTimezoneInput = z.infer<typeof inputSchema>;

export const places_timezone = {
  name: "places_timezone",
  description: "Returns timezone information for the given coordinates or city name. Provide either `city` or both `lat` and `lon`.",
  inputSchema,

  handler: async (rawArgs: Record<string, unknown>) => {
    const args = inputSchema.parse(rawArgs);
    const result = await requiemsRequest({
      method: "GET",
      path: "/v1/places/timezone",
      pathParams: undefined,
      query: { lat: args.lat, lon: args.lon, city: args.city },
      body: undefined,
    });
    return result;
  },
};
