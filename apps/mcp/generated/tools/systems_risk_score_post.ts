// AUTO-GENERATED by scripts/generate.ts — do not edit by hand.
// Source operation: POST /v1/systems/risk/score
// Regenerate with: bun run scripts/generate.ts --input <spec> --output generated

import { z } from "zod";
import { requiemsRequest } from "../runtime";

/**
 * Score a user for risk without the full signal breakdown. Lower latency than /signup/protect, suited for background re-scoring and rate limiting.
 */
export const inputSchema = z.object({
  email: z.string().optional().describe("Email address to include in the risk score."),
  phone: z.string().optional().describe("Phone number in E.164 format."),
  ip_address: z.string().optional().describe("IPv4 or IPv6 address to check."),
});

export type SystemsRiskScorePostInput = z.infer<typeof inputSchema>;

export const systems_risk_score_post = {
  name: "systems_risk_score_post",
  description: "Score a user for risk without the full signal breakdown. Lower latency than /signup/protect, suited for background re-scoring and rate limiting.",
  inputSchema,

  handler: async (rawArgs: Record<string, unknown>) => {
    const args = inputSchema.parse(rawArgs);
    const result = await requiemsRequest({
      method: "POST",
      path: "/v1/systems/risk/score",
      pathParams: undefined,
      query: undefined,
      body: { email: args.email, phone: args.phone, ip_address: args.ip_address },
    });
    return result;
  },
};
