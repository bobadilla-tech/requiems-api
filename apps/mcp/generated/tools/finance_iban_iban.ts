// AUTO-GENERATED by scripts/generate.ts — do not edit by hand.
// Source operation: GET /v1/finance/iban/{iban}
// Regenerate with: bun run scripts/generate.ts --input <spec> --output generated

import { z } from "zod";
import { requiemsRequest } from "../runtime";

/**
 * Validates an IBAN and returns the country, bank code, and account number. Spaces in the input are stripped automatically. Always returns HTTP 200 — check the valid field to determine whether the IBAN is valid.
 */
export const inputSchema = z.object({
  iban: z.string().describe("The IBAN to validate. Spaces are stripped. Case-insensitive."),
});

export type FinanceIbanIbanInput = z.infer<typeof inputSchema>;

export const finance_iban_iban = {
  name: "finance_iban_iban",
  description: "Validates an IBAN and returns the country, bank code, and account number. Spaces in the input are stripped automatically. Always returns HTTP 200 — check the valid field to determine whether the IBAN is valid.",
  inputSchema,

  handler: async (rawArgs: Record<string, unknown>) => {
    const args = inputSchema.parse(rawArgs);
    const result = await requiemsRequest({
      method: "GET",
      path: "/v1/finance/iban/{iban}",
      pathParams: { iban: args.iban },
      query: undefined,
      body: undefined,
    });
    return result;
  },
};
