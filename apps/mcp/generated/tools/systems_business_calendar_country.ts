// AUTO-GENERATED by scripts/generate.ts — do not edit by hand.
// Source operation: GET /v1/systems/business-calendar/{country}
// Regenerate with: bun run scripts/generate.ts --input <spec> --output generated

import { z } from "zod";
import { requiemsRequest } from "../runtime";

/**
 * Retrieve working days, public holidays, and next upcoming holiday for any country and optional year/month. Covers 100+ countries.
 */
export const inputSchema = z.object({
  country: z.string().describe("ISO 3166-1 alpha-2 country code (e.g. DE, US, FR)"),
  year: z.number().int().optional().describe("Year to retrieve the calendar for. Defaults to the current year."),
  month: z.number().int().optional().describe("Month (1–12). When provided, returns month-scoped working day count, total days, weekend days, and filtered holidays."),
});

export type SystemsBusinessCalendarCountryInput = z.infer<typeof inputSchema>;

export const systems_business_calendar_country = {
  name: "systems_business_calendar_country",
  description: "Retrieve working days, public holidays, and next upcoming holiday for any country and optional year/month. Covers 100+ countries.",
  inputSchema,

  handler: async (rawArgs: Record<string, unknown>) => {
    const args = inputSchema.parse(rawArgs);
    const result = await requiemsRequest({
      method: "GET",
      path: "/v1/systems/business-calendar/{country}",
      pathParams: { country: args.country },
      query: { year: args.year, month: args.month },
      body: undefined,
    });
    return result;
  },
};
