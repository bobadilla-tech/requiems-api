Your Private API is Live

<%= t('private_deployment_mailer.deployment_ready.hi_contact_name', contact_name: @deployment_request.contact_name) %>

<%= t('private_deployment_mailer.deployment_ready.your_private_requiems_api', company: @deployment_request.company) %>

YOUR ENDPOINT
  <%= @deployment_request.live_url %>

AUTHENTICATION
Include this header in every request:
  X-Backend-Secret: <%= @deployment_request.tenant_secret %>

Keep this value private. It's the only authentication for your instance.

QUICK START
  curl -H "X-Backend-Secret: <your-secret>" \
    <%= @deployment_request.live_url %>/v1/<%= @deployment_request.selected_services.first %>

MOUNTED SERVICES
  <%= @deployment_request.selected_services.join(", ") %>

API DOCUMENTATION
  <%= @docs_url %>

Questions? Reply to this email or reach us at eliaz@bobadilla.tech.

The Requiems Team
