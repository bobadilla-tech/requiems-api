<div class="min-h-screen flex items-center justify-center bg-gray-50 dark:bg-gray-900 py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md w-full space-y-8">
    <div>
      <%= link_to root_path, class: "flex justify-center mb-6" do %>
        <span class="logo-circle">
          <picture>
            <source srcset="<%= asset_path 'logo.webp' %>" type="image/webp">
            <%= image_tag "logo.png", alt: "Requiems API Logo", class: "h-10 w-auto", width: "39", height: "40" %>
          </picture>
        </span>
      <% end %>
      <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900 dark:text-gray-100">
        <%= t("auth.sign_up.heading") %>
      </h2>
      <p class="mt-2 text-center text-sm text-gray-600 dark:text-gray-400">
        <%= t("auth.sign_up.subheading") %>
      </p>
    </div>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "mt-8 space-y-6" }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="rounded-md shadow-sm space-y-4">
        <div>
          <%= f.label :name, t("auth.sign_up.name"), class: "block text-sm font-medium text-gray-700 dark:text-gray-300" %>
          <%= f.text_field :name, autofocus: true, autocomplete: "name", "aria-invalid": resource.errors[:name].present?, "aria-describedby": ("error_explanation" if resource.errors[:name].present?), class: "mt-1 appearance-none relative block w-full px-3 py-2 border border-gray-300 dark:border-gray-600 placeholder-gray-500 dark:placeholder-gray-400 text-gray-900 dark:text-gray-100 dark:bg-gray-700 rounded-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm", placeholder: t("auth.sign_up.name_placeholder") %>
        </div>

        <div>
          <%= f.label :email, t("auth.sign_up.email"), class: "block text-sm font-medium text-gray-700 dark:text-gray-300" %>
          <%= f.email_field :email, autocomplete: "email", "aria-invalid": resource.errors[:email].present?, "aria-describedby": ("error_explanation" if resource.errors[:email].present?), class: "mt-1 appearance-none relative block w-full px-3 py-2 border border-gray-300 dark:border-gray-600 placeholder-gray-500 dark:placeholder-gray-400 text-gray-900 dark:text-gray-100 dark:bg-gray-700 rounded-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm", placeholder: "you@example.com" %>
        </div>

        <div>
          <%= f.label :company, t("auth.sign_up.company"), class: "block text-sm font-medium text-gray-700 dark:text-gray-300" %>
          <%= f.text_field :company, autocomplete: "organization", "aria-invalid": resource.errors[:company].present?, "aria-describedby": ("error_explanation" if resource.errors[:company].present?), class: "mt-1 appearance-none relative block w-full px-3 py-2 border border-gray-300 dark:border-gray-600 placeholder-gray-500 dark:placeholder-gray-400 text-gray-900 dark:text-gray-100 dark:bg-gray-700 rounded-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm", placeholder: t("auth.sign_up.company_placeholder") %>
        </div>

        <div>
          <%= f.label :password, class: "block text-sm font-medium text-gray-700 dark:text-gray-300" do %>
            <%= t("auth.sign_up.password") %>
            <% if @minimum_password_length %>
              <span class="text-gray-500 dark:text-gray-400 text-xs">(<%= t("auth.sign_up.password_min", min: @minimum_password_length) %>)</span>
            <% end %>
          <% end %>
          <%= f.password_field :password, autocomplete: "new-password", "aria-invalid": resource.errors[:password].present?, "aria-describedby": ("error_explanation" if resource.errors[:password].present?), class: "mt-1 appearance-none relative block w-full px-3 py-2 border border-gray-300 dark:border-gray-600 placeholder-gray-500 dark:placeholder-gray-400 text-gray-900 dark:text-gray-100 dark:bg-gray-700 rounded-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm", placeholder: "••••••••" %>
        </div>

        <div>
          <%= f.label :password_confirmation, t("auth.sign_up.confirm_password"), class: "block text-sm font-medium text-gray-700 dark:text-gray-300" %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", "aria-invalid": resource.errors[:password_confirmation].present?, "aria-describedby": ("error_explanation" if resource.errors[:password_confirmation].present?), class: "mt-1 appearance-none relative block w-full px-3 py-2 border border-gray-300 dark:border-gray-600 placeholder-gray-500 dark:placeholder-gray-400 text-gray-900 dark:text-gray-100 dark:bg-gray-700 rounded-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm", placeholder: "••••••••" %>
        </div>
      </div>

      <div>
        <%= f.submit t("auth.sign_up.submit"), class: "group relative w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 cursor-pointer" %>
      </div>

      <div class="text-center text-sm">
        <%= render "devise/shared/links" %>
      </div>
    <% end %>
  </div>
</div>
