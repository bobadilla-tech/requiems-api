<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
      body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif; line-height: 1.6; color: #333; }
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      .header { background-color: #10b981; color: white; padding: 20px; border-radius: 8px 8px 0 0; }
      .content { background-color: #f9fafb; padding: 30px; border-radius: 0 0 8px 8px; }
      .field { margin-bottom: 20px; }
      .label { font-weight: 600; color: #1f2937; margin-bottom: 5px; }
      .value { background-color: white; padding: 15px; border-radius: 6px; border: 1px solid #e5e7eb; }
      .highlight { background-color: #dbeafe; padding: 15px; border-radius: 6px; border-left: 4px solid #3b82f6; margin: 20px 0; }
      .footer { margin-top: 20px; padding-top: 20px; border-top: 1px solid #e5e7eb; color: #6b7280; font-size: 14px; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1 style="margin: 0; font-size: 24px;">🚀 Enterprise Inquiry</h1>
        <p style="margin: 10px 0 0 0; opacity: 0.9;"><%= t('sales_mailer.enterprise_inquiry.a_potential_enterprise_customer_wants') %></p>
      </div>

      <div class="content">
        <div class="highlight">
          <strong>Action Required:</strong> Respond within 24 hours to maintain our commitment.
        </div>

        <div class="field">
          <div class="label"><%= t('admin.private_deployments.index.company') %></div>
          <div class="value"><strong><%= @inquiry[:company] %></strong></div>
        </div>

        <div class="field">
          <div class="label"><%= t('sales_mailer.enterprise_inquiry.contact_person') %></div>
          <div class="value"><%= @inquiry[:name] %></div>
        </div>

        <div class="field">
          <div class="label"><%= t('home.contact.form.email') %></div>
          <div class="value">
            <a href="mailto:<%= @inquiry[:email] %>"><%= @inquiry[:email] %></a>
          </div>
        </div>

        <% if @inquiry[:message].present? %>
          <div class="field">
            <div class="label"><%= t('sales_mailer.enterprise_inquiry.message_specific_needs') %></div>
            <div class="value"><%= simple_format(@inquiry[:message]) %></div>
          </div>
        <% end %>

        <div class="footer">
          <p><strong>Submitted:</strong> <%= @timestamp.strftime("%B %d, %Y at %I:%M %p %Z") %></p>
          <p style="margin-top: 10px; background-color: #fef3c7; padding: 10px; border-radius: 4px; border-left: 4px solid #f59e0b;">
            ⏰ <strong><%= t('sales_mailer.enterprise_inquiry.reminder') %></strong> Enterprise inquiries should receive a response within 24 hours.
          </p>
        </div>
      </div>
    </div>
  </body>
</html>
