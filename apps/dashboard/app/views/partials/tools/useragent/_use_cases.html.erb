<section class="bg-white dark:bg-gray-900 py-20">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

    <div class="text-center mb-12">
      <h2 class="text-3xl font-bold text-gray-900 dark:text-white"><%= t("tools.useragent.use_cases.heading") %></h2>
      <p class="mt-3 text-gray-500 dark:text-gray-400 max-w-xl mx-auto">
        <%= t("tools.useragent.use_cases.subheading") %>
      </p>
    </div>

    <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
      <% [
        { title: t("tools.useragent.use_cases.analytics_title"),
          desc:  t("tools.useragent.use_cases.analytics_desc"),
          tags:  t("tools.useragent.use_cases.analytics_tags").split(",").map(&:strip) },
        { title: t("tools.useragent.use_cases.adaptive_title"),
          desc:  t("tools.useragent.use_cases.adaptive_desc"),
          tags:  t("tools.useragent.use_cases.adaptive_tags").split(",").map(&:strip) },
        { title: t("tools.useragent.use_cases.bot_title"),
          desc:  t("tools.useragent.use_cases.bot_desc"),
          tags:  t("tools.useragent.use_cases.bot_tags").split(",").map(&:strip) },
        { title: t("tools.useragent.use_cases.security_title"),
          desc:  t("tools.useragent.use_cases.security_desc"),
          tags:  t("tools.useragent.use_cases.security_tags").split(",").map(&:strip) },
        { title: t("tools.useragent.use_cases.testing_title"),
          desc:  t("tools.useragent.use_cases.testing_desc"),
          tags:  t("tools.useragent.use_cases.testing_tags").split(",").map(&:strip) },
        { title: t("tools.useragent.use_cases.personalization_title"),
          desc:  t("tools.useragent.use_cases.personalization_desc"),
          tags:  t("tools.useragent.use_cases.personalization_tags").split(",").map(&:strip) },
      ].each do |uc| %>
        <div class="rounded-xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 p-6 hover:shadow-md transition-shadow">
          <h3 class="font-semibold text-gray-900 dark:text-white mb-2"><%= uc[:title] %></h3>
          <p class="text-sm text-gray-500 dark:text-gray-400 leading-relaxed mb-4"><%= uc[:desc] %></p>
          <div class="flex flex-wrap gap-2">
            <% uc[:tags].each do |tag| %>
              <span class="text-xs px-2 py-0.5 rounded-full bg-gray-100 dark:bg-gray-700 text-gray-600 dark:text-gray-300">
                <%= tag %>
              </span>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>

  </div>
</section>
