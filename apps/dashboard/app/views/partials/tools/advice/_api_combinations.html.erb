<section class="bg-gray-50 dark:bg-gray-800 py-20">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-12">
      <h2 class="text-3xl font-bold text-gray-900 dark:text-white"><%= t("tools.advice.api_combinations.heading") %></h2>
      <p class="mt-3 text-gray-500 dark:text-gray-400 max-w-xl mx-auto">
        <%= t("tools.advice.api_combinations.subheading") %>
      </p>
    </div>
    <div class="grid sm:grid-cols-3 gap-6">
      <% [
        { pair:    t("tools.advice.api_combinations.quotes_pair"),
          benefit: t("tools.advice.api_combinations.quotes_benefit"),
          outcome: t("tools.advice.api_combinations.quotes_outcome"),
          href:    api_path("quotes") },
        { pair:    t("tools.advice.api_combinations.trivia_pair"),
          benefit: t("tools.advice.api_combinations.trivia_benefit"),
          outcome: t("tools.advice.api_combinations.trivia_outcome"),
          href:    api_path("trivia") },
        { pair:    t("tools.advice.api_combinations.sentiment_pair"),
          benefit: t("tools.advice.api_combinations.sentiment_benefit"),
          outcome: t("tools.advice.api_combinations.sentiment_outcome"),
          href:    api_path("sentiment-analysis") },
      ].each do |c| %>
        <a href="<%= c[:href] %>" class="block rounded-xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 p-6 hover:shadow-md transition-shadow no-underline">
          <div class="font-semibold text-gray-900 dark:text-white mb-2 leading-snug"><%= c[:pair] %></div>
          <p class="text-xs text-gray-500 dark:text-gray-400 mb-1">
            <span class="font-medium"><%= t("tools.advice.api_combinations.label_benefit") %></span> <%= c[:benefit] %>
          </p>
          <p class="text-xs text-gray-500 dark:text-gray-400">
            <span class="font-medium"><%= t("tools.advice.api_combinations.label_outcome") %></span> <%= c[:outcome] %>
          </p>
        </a>
      <% end %>
    </div>
  </div>
</section>
