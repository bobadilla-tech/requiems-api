en:
  comparisons:
    hub:
      meta_title: "Requiems API vs Competitors: Full API Comparison 2026"
      meta_description: "See how Requiems API compares to ZeroBounce, NeverBounce, IPstack, AbstractAPI, Neutrinoapi, Kickbox, and more. Side-by-side feature matrix, pricing, and migration guides."
      hero_headline: "How Requiems API Compares"
      hero_subheadline: "One API key. 100+ endpoints. No juggling five vendors and five invoices. See how we stack up against the specialists."
      hero_cta_primary: "Start Free"
      hero_cta_secondary: "View API Docs"
      philosophy_heading: "Why teams switch to Requiems API"
      philosophy_breadth_title: "One API, Everything"
      philosophy_breadth_body: "Email verification, IP geolocation, phone validation, finance data — all under one key, one dashboard, one monthly invoice."
      philosophy_pricing_title: "Transparent Pricing"
      philosophy_pricing_body: "Pay per call, not per product. No separate subscriptions for each data type. Your budget goes further when it's not split across vendors."
      philosophy_dx_title: "Developer-First DX"
      philosophy_dx_body: "Consistent auth, consistent error codes, consistent response shapes across every endpoint. Onboard once, then add more APIs without re-learning anything."
      benchmarks_heading: "At a glance"
      benchmarks_caption: "Requiems API vs. top specialists across key decision criteria."
      benchmarks_feature: "Feature"
      benchmarks_ours: "Requiems API"
      benchmarks_rows:
        - label: "API endpoints"
          ours: "100+"
          zerobounce: "~8"
          neverbounce: "~5"
          ipstack: "~3"
          abstractapi: "~20"
        - label: "Single API key for all services"
          ours: true
          zerobounce: false
          neverbounce: false
          ipstack: false
          abstractapi: false
        - label: "Free tier"
          ours: "500 req/mo"
          zerobounce: "100 credits"
          neverbounce: "1,000 credits"
          ipstack: "100 req/mo"
          abstractapi: "Varies per API"
        - label: "Uptime SLA"
          ours: "99.9%"
          zerobounce: "99.9%"
          neverbounce: "99.9%"
          ipstack: "99.9%"
          abstractapi: "99.99%"
        - label: "Consistent response schema"
          ours: true
          zerobounce: false
          neverbounce: false
          ipstack: false
          abstractapi: false
        - label: "HTTPS / TLS 1.3"
          ours: true
          zerobounce: true
          neverbounce: true
          ipstack: true
          abstractapi: true
        - label: "Bulk / batch endpoint"
          ours: true
          zerobounce: true
          neverbounce: true
          ipstack: false
          abstractapi: false
      benchmarks_columns: {}
      competitors_heading: "Pick your comparison"
      card_cta: "View Full Comparison"
      card_coming_soon: "Coming Soon"
      migration_heading: "Switching is fast"
      migration_body: "Swap your base URL, update your auth header, adjust field names. Most integrations migrate in under an hour. Full migration guides in our docs."
      migration_cta: "Read Migration Docs"
    show:
      cta_primary: "Start Free"
      cta_secondary: "View API Docs"
      features_feature_label: "Feature"
      migration_read_docs: "Read full migration docs"
      cta_see_all: "See All Comparisons"
      cta_headline: "Ready to switch from %{competitor}?"
      cta_body: "500 free API calls per month, no credit card required. Migration takes under an hour."
      uptime_sla: "Uptime SLA"
      latency_p50: "Latency (p50)"
      free_tier: "Free tier"
      api_coverage: "API coverage"
    competitors:
      zerobounce:
        tldr:
          - title: "One key for everything"
            body: "ZeroBounce is email-only. Requiems API covers email, phone, IP, finance, and more — one subscription, one integration."
          - title: "Consistent API shape"
            body: "Every Requiems API endpoint shares the same auth, error codes, and envelope structure. ZeroBounce each product has its own quirks."
          - title: "No credit-pool anxiety"
            body: "ZeroBounce sells credits that expire. Requiems API bills per call with a monthly free tier — use it when you need it."
        features:
          rows:
            - feature: "Syntax validation"
              ours: true
              theirs: true
            - feature: "DNS / MX record check"
              ours: true
              theirs: true
            - feature: "SMTP verification"
              ours: true
              theirs: true
            - feature: "Catch-all detection"
              ours: true
              theirs: true
            - feature: "Disposable email detection"
              ours: true
              theirs: true
            - feature: "Role-based address detection"
              ours: true
              theirs: true
            - feature: "Bulk list verification"
              ours: true
              theirs: true
            - feature: "Real-time API"
              ours: true
              theirs: true
            - feature: "Webhook on completion"
              ours: true
              theirs: true
            - feature: "IP geolocation"
              ours: true
              theirs: false
            - feature: "Phone validation"
              ours: true
              theirs: false
            - feature: "Finance / BIN lookup"
              ours: true
              theirs: false
        benchmarks: {}
        philosophy: {}
        migration:
          steps:
            - "Replace `api.zerobounce.net` base URL with `api.requiems.dev`"
            - "Swap `apikey` query param for `Authorization: Bearer <key>` header"
            - "Map `status` field: ZeroBounce `valid` → Requiems `deliverable`; `invalid` → `undeliverable`"
      neverbounce:
        tldr:
          - title: "Broader coverage"
            body: "NeverBounce is email-only. Requiems API adds phone, IP, finance, and 80+ more endpoints under one key."
          - title: "No enterprise lock-in"
            body: "NeverBounce is owned by ZoomInfo; pricing and roadmap follow enterprise priorities. Requiems API is independent and developer-centric."
          - title: "Simpler integration"
            body: "One auth pattern, one error format, one SDK across all APIs. NeverBounce requires a separate integration per product."
        features:
          rows:
            - feature: "Syntax validation"
              ours: true
              theirs: true
            - feature: "DNS / MX record check"
              ours: true
              theirs: true
            - feature: "SMTP verification"
              ours: true
              theirs: true
            - feature: "Catch-all detection"
              ours: true
              theirs: true
            - feature: "Disposable email detection"
              ours: true
              theirs: true
            - feature: "Role-based address detection"
              ours: true
              theirs: true
            - feature: "Bulk list cleaning"
              ours: true
              theirs: true
            - feature: "Real-time API"
              ours: true
              theirs: true
            - feature: "85+ marketing integrations"
              ours: false
              theirs: true
            - feature: "IP geolocation"
              ours: true
              theirs: false
            - feature: "Phone validation"
              ours: true
              theirs: false
            - feature: "Finance / BIN lookup"
              ours: true
              theirs: false
        benchmarks: {}
        philosophy: {}
        migration:
          steps:
            - "Replace `api.neverbounce.com` base URL with `api.requiems.dev`"
            - "Swap API key from query param to `Authorization: Bearer <key>` header"
            - "Map result codes: `valid` → `deliverable`; `invalid` → `undeliverable`; `disposable` → `risky`"
      kickbox:
        tldr:
          - title: "No proprietary score lock-in"
            body: "Kickbox's Sendex score is a black box. Requiems API returns raw signals so you build your own deliverability logic."
          - title: "Beyond email"
            body: "Kickbox is email-only. Requiems API covers phone, IP, finance, and more — same subscription."
          - title: "SOC 2 + GDPR"
            body: "Both platforms are SOC 2 / GDPR compliant. Requiems API adds per-call audit logs out of the box."
        features:
          rows:
            - feature: "Syntax validation"
              ours: true
              theirs: true
            - feature: "DNS / MX record check"
              ours: true
              theirs: true
            - feature: "SMTP verification"
              ours: true
              theirs: true
            - feature: "Catch-all detection"
              ours: true
              theirs: true
            - feature: "Disposable email detection"
              ours: true
              theirs: true
            - feature: "Role-based address detection"
              ours: true
              theirs: true
            - feature: "Deliverability score"
              ours: true
              theirs: true
            - feature: "Bulk list cleaning"
              ours: true
              theirs: true
            - feature: "Real-time API"
              ours: true
              theirs: true
            - feature: "Deliverability consulting"
              ours: false
              theirs: true
            - feature: "IP geolocation"
              ours: true
              theirs: false
            - feature: "Phone validation"
              ours: true
              theirs: false
        benchmarks: {}
        philosophy: {}
        migration:
          steps:
            - "Replace `api.kickbox.com` base URL with `api.requiems.dev`"
            - "Swap `apikey` query param for `Authorization: Bearer <key>` header"
            - "Map `result` field: `deliverable` stays; `undeliverable` stays; `risky` maps to Requiems `risky`"
      mailboxlayer:
        hero_context: "Email Validation"
        tldr:
          - title: "More endpoints, same simplicity"
            body: "Mailboxlayer has one endpoint. Requiems API has 100+ with the same URL-based simplicity."
          - title: "Better free tier"
            body: "Mailboxlayer free = 100 req/mo. Requiems API free = 500 req/mo with no credit card required."
          - title: "Active development"
            body: "Mailboxlayer is a legacy APILayer product with slow updates. Requiems API ships new APIs monthly."
        features:
          rows:
            - feature: "Syntax validation"
              ours: true
              theirs: true
            - feature: "DNS / MX record check"
              ours: true
              theirs: true
            - feature: "SMTP verification"
              ours: true
              theirs: true
            - feature: "Catch-all detection"
              ours: true
              theirs: false
            - feature: "Disposable email detection"
              ours: true
              theirs: true
            - feature: "Role-based address detection"
              ours: true
              theirs: true
            - feature: "Quality / deliverability score"
              ours: true
              theirs: true
            - feature: "Did-you-mean suggestions"
              ours: false
              theirs: true
            - feature: "Bulk list endpoint"
              ours: true
              theirs: false
            - feature: "IP geolocation"
              ours: true
              theirs: false
            - feature: "Phone validation"
              ours: true
              theirs: false
        benchmarks: {}
        philosophy: {}
        migration:
          steps:
            - "Replace `apilayer.net/api/check` with `api.requiems.dev/v1/email/validate`"
            - "Swap `access_key` query param for `Authorization: Bearer <key>` header"
            - "Map `format_valid` → `syntax_valid`; `smtp_check` → `smtp_valid`; `score` → `deliverability_score`"
      bouncify:
        tldr:
          - title: "API-first vs upload-first"
            body: "Bouncify is optimized for CSV uploads. Requiems API is built for real-time API calls — validate at signup, not after the fact."
          - title: "No credit expiry"
            body: "Bouncify sells credits; unused credits expire. Requiems API charges per call with a free monthly allowance that resets."
          - title: "100+ endpoints"
            body: "Bouncify is email-only. Requiems API covers email, phone, IP, and finance in one subscription."
        features:
          rows:
            - feature: "Syntax validation"
              ours: true
              theirs: true
            - feature: "DNS / MX record check"
              ours: true
              theirs: true
            - feature: "SMTP verification"
              ours: true
              theirs: true
            - feature: "Catch-all detection"
              ours: true
              theirs: true
            - feature: "Disposable email detection"
              ours: true
              theirs: true
            - feature: "Spam-trap removal"
              ours: true
              theirs: true
            - feature: "Anti-greylisting"
              ours: false
              theirs: true
            - feature: "Real-time API"
              ours: true
              theirs: true
            - feature: "Bulk list upload"
              ours: true
              theirs: true
            - feature: "IP geolocation"
              ours: true
              theirs: false
            - feature: "Phone validation"
              ours: true
              theirs: false
        benchmarks: {}
        philosophy: {}
        migration:
          steps:
            - "Move from batch upload workflow to real-time API call on form submission"
            - "Use `Authorization: Bearer <key>` header with `api.requiems.dev/v1/email/validate`"
            - "Map result fields: `deliverable` stays; `invalid` → `undeliverable`; `accept_all` → `catch_all`"
      ipstack:
        tldr:
          - title: "One vendor for IP + email + phone"
            body: "IPstack is geolocation-only. Requiems API provides the same location data plus email verification, phone validation, and more."
          - title: "No legacy APILayer baggage"
            body: "IPstack runs on APILayer infrastructure with slow release cadence. Requiems API ships improvements continuously."
          - title: "Richer free tier"
            body: "IPstack free = 100 req/mo. Requiems API free = 500 req/mo across every endpoint type."
        features:
          rows:
            - feature: "Country / region / city"
              ours: true
              theirs: true
            - feature: "Latitude / longitude"
              ours: true
              theirs: true
            - feature: "Timezone detection"
              ours: true
              theirs: true
            - feature: "Currency info"
              ours: true
              theirs: true
            - feature: "ISP / ASN lookup"
              ours: true
              theirs: true
            - feature: "Proxy / VPN / Tor detection"
              ours: true
              theirs: true
            - feature: "Threat level scoring"
              ours: true
              theirs: true
            - feature: "IPv6 support"
              ours: true
              theirs: true
            - feature: "Bulk IP lookup"
              ours: true
              theirs: false
            - feature: "Email verification"
              ours: true
              theirs: false
            - feature: "Phone validation"
              ours: true
              theirs: false
        benchmarks: {}
        philosophy: {}
        migration:
          steps:
            - "Replace `api.ipstack.com/{ip}` with `api.requiems.dev/v1/ip/lookup?ip={ip}`"
            - "Swap `access_key` query param for `Authorization: Bearer <key>` header"
            - "Most response fields are identical; map `location.geoname_id` to `geoname_id` at root level"
      abstractapi:
        tldr:
          - title: "One key, not twenty"
            body: "AbstractAPI gives you one key per API product. Requiems API gives you one key for all 100+ endpoints."
          - title: "One invoice"
            body: "AbstractAPI has a separate plan per product — you end up with multiple subscriptions. Requiems API is one bill."
          - title: "Consistent schema"
            body: "AbstractAPI response shapes vary per product. Requiems API uses a consistent envelope, error format, and auth method across everything."
        features:
          rows:
            - feature: "Email verification"
              ours: true
              theirs: true
            - feature: "IP geolocation"
              ours: true
              theirs: true
            - feature: "Phone validation"
              ours: true
              theirs: true
            - feature: "Company enrichment"
              ours: false
              theirs: true
            - feature: "Public holidays"
              ours: true
              theirs: true
            - feature: "Exchange rates / finance"
              ours: true
              theirs: true
            - feature: "Single API key for all services"
              ours: true
              theirs: false
            - feature: "Unified billing"
              ours: true
              theirs: false
            - feature: "Consistent response schema"
              ours: true
              theirs: false
            - feature: "100+ endpoints"
              ours: true
              theirs: false
        benchmarks: {}
        philosophy: {}
        migration:
          steps:
            - "Replace `emailvalidation.abstractapi.com`, `ipgeolocation.abstractapi.com`, etc. with `api.requiems.dev/v1/{category}/{endpoint}`"
            - "Swap `api_key` query param for `Authorization: Bearer <key>` header — same key works for every endpoint"
            - "Response field names are similar; consult per-endpoint migration notes in our docs"
      neutrinoapi:
        tldr:
          - title: "More endpoints"
            body: "Neutrinoapi covers ~30 endpoints. Requiems API covers 100+ with faster release cadence."
          - title: "Modern DX"
            body: "Neutrinoapi's API design reflects its 2013 origins. Requiems API uses modern REST conventions, JSON:API-inspired envelopes, and OpenAPI 3.1 docs."
          - title: "Transparent pricing"
            body: "Neutrinoapi uses a credit system with per-endpoint credit costs. Requiems API bills uniformly per API call."
        features:
          rows:
            - feature: "Email validation"
              ours: true
              theirs: true
            - feature: "Phone validation"
              ours: true
              theirs: true
            - feature: "IP geolocation"
              ours: true
              theirs: true
            - feature: "IP blocklist / reputation"
              ours: true
              theirs: true
            - feature: "Domain / DNS lookup"
              ours: true
              theirs: true
            - feature: "Browser automation"
              ours: false
              theirs: true
            - feature: "HTML rendering / PDF"
              ours: false
              theirs: true
            - feature: "SMS / phone playback"
              ours: false
              theirs: true
            - feature: "QR code generation"
              ours: true
              theirs: true
            - feature: "Finance / BIN lookup"
              ours: true
              theirs: true
            - feature: "100+ endpoints"
              ours: true
              theirs: false
            - feature: "OpenAPI 3.1 spec"
              ours: true
              theirs: false
        benchmarks: {}
        philosophy: {}
        migration:
          steps:
            - "Replace `neutrinoapi.net/api/{endpoint}` with `api.requiems.dev/v1/{category}/{endpoint}`"
            - "Swap user-id + api-key combo for `Authorization: Bearer <key>` header"
            - "Map credit-based rate limits to per-call metering in your Requiems dashboard"
      api_ninjas:
        tldr:
          - title: "Validation depth"
            body: "API Ninjas has breadth but shallow validation APIs. Requiems API's email, phone, and IP validation goes deeper — SMTP checks, HLR lookup, threat scoring."
          - title: "Real-time data"
            body: "API Ninjas sources some data from static datasets. Requiems API does live checks for email, phone, and IP on every call."
          - title: "Enterprise-ready billing"
            body: "Both offer volume pricing. Requiems API adds per-team usage dashboards, per-key rate limits, and invoice-based billing."
        features:
          rows:
            - feature: "Email validation"
              ours: true
              theirs: false
            - feature: "Phone validation"
              ours: true
              theirs: true
            - feature: "IP geolocation"
              ours: true
              theirs: true
            - feature: "Finance / stock / crypto data"
              ours: true
              theirs: true
            - feature: "Live SMTP check"
              ours: true
              theirs: false
            - feature: "IP threat detection"
              ours: true
              theirs: false
            - feature: "Earnings transcripts"
              ours: false
              theirs: true
            - feature: "Domain availability"
              ours: true
              theirs: true
            - feature: "135+ endpoints"
              ours: false
              theirs: true
            - feature: "OpenAPI 3.1 spec"
              ours: true
              theirs: false
        benchmarks: {}
        philosophy: {}
        migration:
          steps:
            - "Replace `api.api-ninjas.com/v1/{endpoint}` with `api.requiems.dev/v1/{category}/{endpoint}`"
            - "Swap `X-Api-Key` header for `Authorization: Bearer <key>`"
            - "Consult per-endpoint migration docs for field mapping"
      numverify:
        tldr:
          - title: "Beyond phone"
            body: "Numverify is phone-only. Requiems API adds email verification, IP geolocation, finance, and 80+ more endpoints."
          - title: "Better free tier"
            body: "Numverify free = 100 req/mo. Requiems API free = 500 req/mo, no credit card."
          - title: "Active development"
            body: "Numverify is a legacy APILayer product. Requiems API ships new APIs and improvements every sprint."
        features:
          rows:
            - feature: "Phone number formatting"
              ours: true
              theirs: true
            - feature: "Country / location detection"
              ours: true
              theirs: true
            - feature: "Carrier identification"
              ours: true
              theirs: true
            - feature: "Line type (mobile/landline/VoIP)"
              ours: true
              theirs: true
            - feature: "232-country coverage"
              ours: true
              theirs: true
            - feature: "HLR / live reachability check"
              ours: true
              theirs: false
            - feature: "Bulk phone validation"
              ours: true
              theirs: false
            - feature: "Email verification"
              ours: true
              theirs: false
            - feature: "IP geolocation"
              ours: true
              theirs: false
        benchmarks: {}
        philosophy: {}
        migration:
          steps:
            - "Replace `apilayer.net/api/validate` with `api.requiems.dev/v1/phone/validate`"
            - "Swap `access_key` query param for `Authorization: Bearer <key>` header"
            - "Map `line_type` values: `mobile` stays; `landline` stays; `special_services` → `other`"
    competitor_card:
      requiems_api_vs_display_name: "Requiems API vs %{display_name}"
