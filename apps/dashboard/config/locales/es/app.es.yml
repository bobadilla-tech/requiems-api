es:
  app:
    lib:
      app_config:
        lemonsqueezy_store_id_must_be_numeric: "LEMONSQUEEZY_STORE_ID debe ser numérico."
    services:
      d1_sync_service:
        invalid_api_management_key: "Clave de administración de API no válida"
