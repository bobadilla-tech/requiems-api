api_id: fitness-exercises
api_name: Fitness Exercises
description: Browse 1,500+ exercises with step-by-step instructions, target muscles, secondary muscles, equipment requirements, and body part filters. Supports full-text search and pagination.
base_url: https://requiems.xyz

overview:
  use_cases:
    - Fitness and workout apps
    - Personal training platforms
    - Health and wellness dashboards
    - Exercise recommendation engines
    - Gym management software

  features:
    - "1,500+ exercises across all major muscle groups"
    - Filter by body part, equipment, or muscle name
    - Full-text search by exercise name
    - Step-by-step instructions for every exercise
    - Random exercise endpoint with optional filters
    - Metadata endpoints listing all valid filter values

endpoints:
  - name: List Exercises
    method: GET
    path: /v1/health/exercises
    description: "Returns a paginated list of exercises. All filter parameters are optional and combinable."

    parameters:
      - name: body_part
        type: string
        required: false
        location: query
        description: "Filter by body part (e.g. chest, back, upper legs). Use /v1/health/body-parts for valid values."
        example: chest
      - name: equipment
        type: string
        required: false
        location: query
        description: "Filter by equipment type (e.g. barbell, dumbbell, body weight). Use /v1/health/equipment for valid values."
        example: barbell
      - name: muscle
        type: string
        required: false
        location: query
        description: "Filter by target or secondary muscle (e.g. biceps, glutes). Use /v1/health/muscles for valid values."
        example: biceps
      - name: search
        type: string
        required: false
        location: query
        description: Full-text search on exercise name.
        example: bench press
      - name: page
        type: integer
        required: false
        location: query
        description: "Page number (default: 1)"
        example: 1
      - name: per_page
        type: integer
        required: false
        location: query
        description: "Results per page, 1–100 (default: 20)"
        example: 20

    response_example: |
      {
        "data": {
          "items": [
            {
              "id": 1,
              "name": "barbell bench press",
              "body_parts": ["chest"],
              "equipment": ["barbell"],
              "target_muscles": ["pectorals"],
              "secondary_muscles": ["triceps", "deltoids"],
              "instructions": [
                "Lie flat on a bench and grip the barbell slightly wider than shoulder-width.",
                "Lower the bar to your chest under control.",
                "Press the bar back up to full arm extension.",
                "Repeat for the desired number of repetitions."
              ]
            }
          ],
          "total": 312,
          "page": 1,
          "per_page": 20
        },
        "metadata": {
          "timestamp": "2026-01-01T00:00:00Z"
        }
      }

    response_fields:
      - name: items
        type: array<object>
        description: Array of exercise objects for the current page
      - name: items[].id
        type: integer
        description: Unique exercise identifier
      - name: items[].name
        type: string
        description: Exercise name
      - name: items[].body_parts
        type: array<string>
        description: Body part categories involved
      - name: items[].equipment
        type: array<string>
        description: Equipment required
      - name: items[].target_muscles
        type: array<string>
        description: Primary muscles targeted
      - name: items[].secondary_muscles
        type: array<string>
        description: Secondary muscles engaged
      - name: items[].instructions
        type: array<string>
        description: Ordered step-by-step instructions
      - name: total
        type: integer
        description: Total number of exercises matching the filters
      - name: page
        type: integer
        description: Current page number
      - name: per_page
        type: integer
        description: Number of results per page

    errors:
      - code: bad_request
        status: 400
        description: A query parameter has an invalid value (e.g. per_page out of range).
      - code: internal_error
        status: 500
        description: Unexpected server error.

    code_examples:
      curl: |
        curl "https://requiems.xyz/v1/health/exercises?body_part=chest&equipment=barbell&per_page=10" \
          -H "requiems-api-key: YOUR_API_KEY"

      python: |
        import requests

        url = "https://requiems.xyz/v1/health/exercises"
        headers = {"requiems-api-key": "YOUR_API_KEY"}
        params = {"body_part": "chest", "equipment": "barbell", "per_page": 10}

        response = requests.get(url, headers=headers, params=params)
        data = response.json()
        for exercise in data["data"]["items"]:
            print(exercise["name"])

      javascript: |
        const params = new URLSearchParams({ body_part: 'chest', equipment: 'barbell', per_page: 10 });
        const response = await fetch(`https://requiems.xyz/v1/health/exercises?${params}`, {
          headers: { 'requiems-api-key': 'YOUR_API_KEY' }
        });
        const { data } = await response.json();
        data.items.forEach(e => console.log(e.name));

      ruby: |
        require 'net/http'
        require 'json'

        uri = URI('https://requiems.xyz/v1/health/exercises')
        uri.query = URI.encode_www_form(body_part: 'chest', equipment: 'barbell', per_page: 10)
        request = Net::HTTP::Get.new(uri)
        request['requiems-api-key'] = 'YOUR_API_KEY'

        response = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) { |http| http.request(request) }
        data = JSON.parse(response.body)
        data['data']['items'].each { |e| puts e['name'] }

  - name: Exercise by ID
    method: GET
    path: /v1/health/exercises/{id}
    description: Returns a single exercise by its numeric ID.

    parameters:
      - name: id
        type: integer
        required: true
        location: path
        description: Numeric exercise ID
        example: 1

    response_example: |
      {
        "data": {
          "id": 1,
          "name": "barbell bench press",
          "body_parts": ["chest"],
          "equipment": ["barbell"],
          "target_muscles": ["pectorals"],
          "secondary_muscles": ["triceps", "deltoids"],
          "instructions": [
            "Lie flat on a bench and grip the barbell slightly wider than shoulder-width.",
            "Lower the bar to your chest under control.",
            "Press the bar back up to full arm extension.",
            "Repeat for the desired number of repetitions."
          ]
        },
        "metadata": {
          "timestamp": "2026-01-01T00:00:00Z"
        }
      }

    response_fields:
      - name: id
        type: integer
        description: Unique exercise identifier
      - name: name
        type: string
        description: Exercise name
      - name: body_parts
        type: array<string>
        description: Body part categories involved
      - name: equipment
        type: array<string>
        description: Equipment required
      - name: target_muscles
        type: array<string>
        description: Primary muscles targeted
      - name: secondary_muscles
        type: array<string>
        description: Secondary muscles engaged
      - name: instructions
        type: array<string>
        description: Ordered step-by-step instructions

    errors:
      - code: bad_request
        status: 400
        description: The id parameter is not a positive integer.
      - code: not_found
        status: 404
        description: No exercise exists with the given ID.
      - code: internal_error
        status: 500
        description: Unexpected server error.

    code_examples:
      curl: |
        curl https://requiems.xyz/v1/health/exercises/1 \
          -H "requiems-api-key: YOUR_API_KEY"

      python: |
        import requests

        response = requests.get(
            "https://requiems.xyz/v1/health/exercises/1",
            headers={"requiems-api-key": "YOUR_API_KEY"}
        )
        print(response.json()["data"]["name"])

      javascript: |
        const response = await fetch('https://requiems.xyz/v1/health/exercises/1', {
          headers: { 'requiems-api-key': 'YOUR_API_KEY' }
        });
        const { data } = await response.json();
        console.log(data.name);

      ruby: |
        require 'net/http'
        require 'json'

        uri = URI('https://requiems.xyz/v1/health/exercises/1')
        request = Net::HTTP::Get.new(uri)
        request['requiems-api-key'] = 'YOUR_API_KEY'
        response = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) { |http| http.request(request) }
        puts JSON.parse(response.body)['data']['name']

  - name: Random Exercise
    method: GET
    path: /v1/health/exercises/random
    description: "Returns a single randomly selected exercise. Accepts the same filter parameters as the list endpoint, so you can get a random chest exercise, a random bodyweight exercise, etc."

    parameters:
      - name: body_part
        type: string
        required: false
        location: query
        description: "Restrict random selection to this body part."
        example: back
      - name: equipment
        type: string
        required: false
        location: query
        description: Restrict random selection to this equipment type.
        example: body weight
      - name: muscle
        type: string
        required: false
        location: query
        description: Restrict random selection to exercises targeting this muscle.
        example: glutes
      - name: search
        type: string
        required: false
        location: query
        description: Restrict random selection to exercises matching this search term.
        example: curl

    response_example: |
      {
        "data": {
          "id": 42,
          "name": "dumbbell curl",
          "body_parts": ["upper arms"],
          "equipment": ["dumbbell"],
          "target_muscles": ["biceps"],
          "secondary_muscles": ["brachialis"],
          "instructions": [
            "Stand with a dumbbell in each hand, arms fully extended.",
            "Curl the weights toward your shoulders, keeping your elbows close to your torso.",
            "Squeeze at the top, then lower the weights slowly.",
            "Repeat for the desired number of repetitions."
          ]
        },
        "metadata": {
          "timestamp": "2026-01-01T00:00:00Z"
        }
      }

    errors:
      - code: not_found
        status: 404
        description: No exercises match the given filters.
      - code: internal_error
        status: 500
        description: Unexpected server error.

    code_examples:
      curl: |
        curl "https://requiems.xyz/v1/health/exercises/random?body_part=back" \
          -H "requiems-api-key: YOUR_API_KEY"

      python: |
        import requests

        response = requests.get(
            "https://requiems.xyz/v1/health/exercises/random",
            headers={"requiems-api-key": "YOUR_API_KEY"},
            params={"body_part": "back"}
        )
        print(response.json()["data"]["name"])

      javascript: |
        const response = await fetch(
          'https://requiems.xyz/v1/health/exercises/random?body_part=back',
          { headers: { 'requiems-api-key': 'YOUR_API_KEY' } }
        );
        const { data } = await response.json();
        console.log(data.name, data.instructions);

      ruby: |
        require 'net/http'
        require 'json'

        uri = URI('https://requiems.xyz/v1/health/exercises/random')
        uri.query = URI.encode_www_form(body_part: 'back')
        request = Net::HTTP::Get.new(uri)
        request['requiems-api-key'] = 'YOUR_API_KEY'
        response = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) { |http| http.request(request) }
        puts JSON.parse(response.body)['data']['name']

  - name: List Body Parts
    method: GET
    path: /v1/health/body-parts
    description: "Returns a sorted list of all distinct body part values present in the dataset. Use these as valid values for the body_part filter."

    response_example: |
      {
        "data": {
          "items": ["back", "cardio", "chest", "lower arms", "lower legs", "neck", "shoulders", "upper arms", "upper legs", "waist"],
          "total": 10
        },
        "metadata": {
          "timestamp": "2026-01-01T00:00:00Z"
        }
      }

    response_fields:
      - name: items
        type: array<string>
        description: Sorted list of all distinct body part names
      - name: total
        type: integer
        description: Total number of distinct body parts

    errors:
      - code: internal_error
        status: 500
        description: Unexpected server error.

    code_examples:
      curl: |
        curl https://requiems.xyz/v1/health/body-parts \
          -H "requiems-api-key: YOUR_API_KEY"

      python: |
        import requests

        response = requests.get(
            "https://requiems.xyz/v1/health/body-parts",
            headers={"requiems-api-key": "YOUR_API_KEY"}
        )
        print(response.json()["data"]["items"])

      javascript: |
        const response = await fetch('https://requiems.xyz/v1/health/body-parts', {
          headers: { 'requiems-api-key': 'YOUR_API_KEY' }
        });
        const { data } = await response.json();
        console.log(data.items);

      ruby: |
        require 'net/http'
        require 'json'

        uri = URI('https://requiems.xyz/v1/health/body-parts')
        request = Net::HTTP::Get.new(uri)
        request['requiems-api-key'] = 'YOUR_API_KEY'
        response = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) { |http| http.request(request) }
        puts JSON.parse(response.body)['data']['items'].inspect

  - name: List Equipment
    method: GET
    path: /v1/health/equipment
    description: "Returns a sorted list of all distinct equipment types. Use these as valid values for the equipment filter."

    response_example: |
      {
        "data": {
          "items": ["band", "barbell", "body weight", "cable", "dumbbell", "ez barbell", "kettlebell", "medicine ball", "stability ball", "tire"],
          "total": 28
        },
        "metadata": {
          "timestamp": "2026-01-01T00:00:00Z"
        }
      }

    response_fields:
      - name: items
        type: array<string>
        description: Sorted list of all distinct equipment names
      - name: total
        type: integer
        description: Total number of distinct equipment types

    errors:
      - code: internal_error
        status: 500
        description: Unexpected server error.

    code_examples:
      curl: |
        curl https://requiems.xyz/v1/health/equipment \
          -H "requiems-api-key: YOUR_API_KEY"

      python: |
        import requests

        response = requests.get(
            "https://requiems.xyz/v1/health/equipment",
            headers={"requiems-api-key": "YOUR_API_KEY"}
        )
        print(response.json()["data"]["items"])

      javascript: |
        const response = await fetch('https://requiems.xyz/v1/health/equipment', {
          headers: { 'requiems-api-key': 'YOUR_API_KEY' }
        });
        const { data } = await response.json();
        console.log(data.items);

      ruby: |
        require 'net/http'
        require 'json'

        uri = URI('https://requiems.xyz/v1/health/equipment')
        request = Net::HTTP::Get.new(uri)
        request['requiems-api-key'] = 'YOUR_API_KEY'
        response = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) { |http| http.request(request) }
        puts JSON.parse(response.body)['data']['items'].inspect

  - name: List Muscles
    method: GET
    path: /v1/health/muscles
    description: "Returns a sorted list of all distinct muscle names (combining target and secondary muscles). Use these as valid values for the muscle filter."

    response_example: |
      {
        "data": {
          "items": ["biceps", "calves", "deltoids", "glutes", "hamstrings", "lats", "pectorals", "quadriceps", "traps", "triceps"],
          "total": 51
        },
        "metadata": {
          "timestamp": "2026-01-01T00:00:00Z"
        }
      }

    response_fields:
      - name: items
        type: array<string>
        description: Sorted list of all distinct muscle names
      - name: total
        type: integer
        description: Total number of distinct muscles

    errors:
      - code: internal_error
        status: 500
        description: Unexpected server error.

    code_examples:
      curl: |
        curl https://requiems.xyz/v1/health/muscles \
          -H "requiems-api-key: YOUR_API_KEY"

      python: |
        import requests

        response = requests.get(
            "https://requiems.xyz/v1/health/muscles",
            headers={"requiems-api-key": "YOUR_API_KEY"}
        )
        print(response.json()["data"]["items"])

      javascript: |
        const response = await fetch('https://requiems.xyz/v1/health/muscles', {
          headers: { 'requiems-api-key': 'YOUR_API_KEY' }
        });
        const { data } = await response.json();
        console.log(data.items);

      ruby: |
        require 'net/http'
        require 'json'

        uri = URI('https://requiems.xyz/v1/health/muscles')
        request = Net::HTTP::Get.new(uri)
        request['requiems-api-key'] = 'YOUR_API_KEY'
        response = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) { |http| http.request(request) }
        puts JSON.parse(response.body)['data']['items'].inspect

  - name: Batch Get Exercises
    method: POST
    path: /v1/health/exercises/batch
    description: "Fetches up to 50 exercises by their numeric IDs in a single request. IDs that do not exist are silently skipped. Results are returned in the same order as the input array."

    parameters:
      - name: ids
        type: array<number>
        required: true
        location: body
        description: "Array of numeric exercise IDs to fetch. Must contain between 1 and 50 items. Each ID must be a positive integer."
        example: "[1, 7, 42]"

    request_example: |
      {
        "ids": [1, 7, 42]
      }

    response_example: |
      {
        "data": {
          "results": [
            {
              "id": 1,
              "name": "barbell bench press",
              "body_parts": ["chest"],
              "equipment": ["barbell"],
              "target_muscles": ["pectorals"],
              "secondary_muscles": ["triceps", "deltoids"],
              "instructions": [
                "Lie flat on a bench and grip the barbell slightly wider than shoulder-width.",
                "Lower the bar to your chest under control.",
                "Press the bar back up to full arm extension.",
                "Repeat for the desired number of repetitions."
              ]
            },
            {
              "id": 7,
              "name": "dumbbell curl",
              "body_parts": ["upper arms"],
              "equipment": ["dumbbell"],
              "target_muscles": ["biceps"],
              "secondary_muscles": ["brachialis"],
              "instructions": [
                "Stand with a dumbbell in each hand, arms fully extended.",
                "Curl the weights toward your shoulders, keeping your elbows close to your torso.",
                "Squeeze at the top, then lower the weights slowly.",
                "Repeat for the desired number of repetitions."
              ]
            }
          ],
          "total": 2
        },
        "metadata": {
          "timestamp": "2026-01-01T00:00:00Z"
        }
      }

    response_fields:
      - name: results
        type: array<object>
        description: Exercises found for the given IDs, in input order. IDs not found are omitted.
      - name: results[].id
        type: integer
        description: Unique exercise identifier
      - name: results[].name
        type: string
        description: Exercise name
      - name: results[].body_parts
        type: array<string>
        description: Body part categories involved
      - name: results[].equipment
        type: array<string>
        description: Equipment required
      - name: results[].target_muscles
        type: array<string>
        description: Primary muscles targeted
      - name: results[].secondary_muscles
        type: array<string>
        description: Secondary muscles engaged
      - name: results[].instructions
        type: array<string>
        description: Ordered step-by-step instructions
      - name: total
        type: integer
        description: Number of exercises returned

    errors:
      - code: bad_request
        status: 400
        description: The request body is missing or not valid JSON.
      - code: validation_failed
        status: 422
        description: "The ids array is empty, exceeds 50 items, or contains a non-positive integer."

    code_examples:
      curl: |
        curl -X POST https://requiems.xyz/v1/health/exercises/batch \
          -H "requiems-api-key: YOUR_API_KEY" \
          -H "Content-Type: application/json" \
          -d '{"ids": [1, 7, 42]}'

      python: |
        import requests

        response = requests.post(
            "https://requiems.xyz/v1/health/exercises/batch",
            headers={"requiems-api-key": "YOUR_API_KEY"},
            json={"ids": [1, 7, 42]}
        )
        for exercise in response.json()["data"]["results"]:
            print(exercise["name"])

      javascript: |
        const response = await fetch('https://requiems.xyz/v1/health/exercises/batch', {
          method: 'POST',
          headers: {
            'requiems-api-key': 'YOUR_API_KEY',
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({ ids: [1, 7, 42] })
        });
        const { data } = await response.json();
        data.results.forEach(e => console.log(e.name));

      ruby: |
        require 'net/http'
        require 'json'

        uri = URI('https://requiems.xyz/v1/health/exercises/batch')
        request = Net::HTTP::Post.new(uri)
        request['requiems-api-key'] = 'YOUR_API_KEY'
        request['Content-Type'] = 'application/json'
        request.body = { ids: [1, 7, 42] }.to_json

        response = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) { |http| http.request(request) }
        JSON.parse(response.body)['data']['results'].each { |e| puts e['name'] }

faq:
  - question: How do I find valid filter values?
    answer: "Call /v1/health/body-parts, /v1/health/equipment, or /v1/health/muscles to get the exact strings accepted by the body_part, equipment, and muscle query parameters."

  - question: Are the IDs stable?
    answer: "Yes. IDs are stable database integers. You can store them to reference specific exercises later. Re-running the seeder updates existing records without changing IDs."

  - question: Can I combine filters?
    answer: "Yes. You can combine body_part, equipment, muscle, and search in a single request. All active filters are ANDed together."

  - question: Is there a GIF or image for each exercise?
    answer: "No. This API intentionally excludes media files. Only text data (names, muscles, instructions) is served."

  - question: What happens if some IDs in a batch request do not exist?
    answer: "Non-existent IDs are silently skipped. The response only includes exercises that were found. Check the total field to verify how many were returned."
