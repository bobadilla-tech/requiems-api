api_id: trivia
api_name: Trivia
description: >-
  Get random trivia questions with multiple-choice answers. Filter by category and difficulty to target specific
  question pools.
base_url: https://requiems.xyz
overview:
  use_cases:
    - Quiz games and trivia applications
    - Educational platforms and learning apps
    - Social and party game features
    - Daily challenge widgets
    - Knowledge-testing integrations
  features:
    - 10 categories — science, history, geography, sports, music, movies, literature, math, technology, nature
    - Three difficulty levels — easy, medium, hard
    - Four answer options per question with the correct answer always included
    - Optional category and difficulty filtering
    - Each request returns a randomly selected question from the matching pool
endpoints:
  - name: Get Trivia Question
    method: GET
    path: /v1/entertainment/trivia
    description: >-
      Returns a random trivia question with multiple-choice answers. Use the optional category and difficulty query
      parameters to filter the question pool.
    parameters:
      - name: category
        location: query
        type: string
        required: false
        description: >-
          Filter by category. One of: science, history, geography, sports, music, movies, literature, math, technology,
          nature.
      - name: difficulty
        location: query
        type: string
        required: false
        description: "Filter by difficulty. One of: easy, medium, hard."
    response_example: |
      {
        "data": {
          "question": "What is the largest planet in our solar system?",
          "options": ["Earth", "Jupiter", "Saturn", "Mars"],
          "answer": "Jupiter",
          "category": "science",
          "difficulty": "easy"
        },
        "metadata": {
          "timestamp": "2026-01-01T00:00:00Z"
        }
      }
    response_fields:
      - name: question
        type: string
        description: The trivia question text
      - name: options
        type: array<string>
        description: Four multiple-choice answer options
      - name: answer
        type: string
        description: The correct answer — always one of the values in options
      - name: category
        type: string
        description: The category the question belongs to
      - name: difficulty
        type: string
        description: The difficulty level of the question (easy, medium, or hard)
    errors:
      - status: 400
        code: bad_request
        description: An invalid category or difficulty value was provided
      - status: 404
        code: not_found
        description: No questions match the given category and difficulty combination
      - status: 401
        code: unauthorized
        description: Missing API key
      - status: 403
        code: forbidden
        description: Invalid or revoked API key
    code_examples:
      curl: |
        # Random question (no filters)
        curl "https://requiems.xyz/v1/entertainment/trivia" \
          -H "requiems-api-key: YOUR_API_KEY"

        # Filtered by category and difficulty
        curl "https://requiems.xyz/v1/entertainment/trivia?category=science&difficulty=medium" \
          -H "requiems-api-key: YOUR_API_KEY"
      python: |
        import requests

        url = "https://requiems.xyz/v1/entertainment/trivia"
        headers = {"requiems-api-key": "YOUR_API_KEY"}
        params = {"category": "science", "difficulty": "medium"}

        response = requests.get(url, headers=headers, params=params)
        data = response.json()["data"]
        print(data["question"])
        print("Options:", data["options"])
        print("Answer:", data["answer"])
      javascript: |
        const response = await fetch(
          'https://requiems.xyz/v1/entertainment/trivia?category=science&difficulty=medium',
          { headers: { 'requiems-api-key': 'YOUR_API_KEY' } }
        );
        const { data } = await response.json();
        console.log(data.question);
        console.log('Options:', data.options);
        console.log('Answer:', data.answer);
      ruby: |
        require 'net/http'
        require 'json'

        uri = URI('https://requiems.xyz/v1/entertainment/trivia')
        uri.query = URI.encode_www_form(category: 'science', difficulty: 'medium')
        request = Net::HTTP::Get.new(uri)
        request['requiems-api-key'] = 'YOUR_API_KEY'

        response = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) do |http|
          http.request(request)
        end

        data = JSON.parse(response.body)['data']
        puts data['question']
        puts "Answer: #{data['answer']}"
  - name: Get Batch Trivia Questions
    method: POST
    path: /v1/entertainment/trivia/batch
    description: Returns up to 50 trivia questions in a single request.
    parameters:
      - name: filters
        type: array<object>
        required: true
        location: body
        description: "Array of filters to get (min: 1, max: 50)."
        example: |
          [{"category": "sports", "difficulty": "easy"},{"category": "history", "difficulty": "medium"}]
    request_example: |
      {
        "filters": [{"category": "sports", "difficulty": "easy"},{"category": "history", "difficulty": "medium"}]
      }
    response_example: |
      {
        "data": {
          "results": [
            {
              "category": "sports",
              "difficulty": "easy",
              "data": {
                "question": "In which country did the ancient Olympic Games originate?",
                "options": [
                  "Italy",
                  "Turkey",
                  "Greece",
                  "Egypt"
                ],
                "answer": "Greece",
                "category": "sports",
                "difficulty": "easy"
              }
            },
            {
              "category": "history",
              "difficulty": "medium",
              "data": {
                "question": "In what year was the Magna Carta signed?",
                "options": [
                  "1215",
                  "1066",
                  "1415",
                  "1305"
                ],
                "answer": "1215",
                "category": "history",
                "difficulty": "medium"
              }
            }
          ],
          "total": 2
        },
        "metadata": {
          "timestamp": "2026-05-22T22:27:35Z"
        }
      }
    response_fields:
      - name: results
        type: array<object>
        description: List of trivia questions returned in the same order as the input filters.
      - name: results[].category
        type: string
        description: The topic filter used to retrieve the trivia question.
      - name: results[].difficulty
        type: string
        description: The complexity level filter used to retrieve the trivia question.
      - name: results[].error
        type: string
        description: Describes why the question could not be retrieved for this filter. Omitted if the query was successful.
      - name: results[].data
        type: object
        description: The trivia question returned for this filter. Empty if the query failed.
      - name: results[].data.question
        type: string
        description: The trivia question text.
      - name: results[].data.options
        type: array<string>
        description: List of possible answers for the question.
      - name: results[].data.answer
        type: string
        description: The correct answer to the trivia question.
      - name: results[].data.category
        type: string
        description: The category the trivia question belongs to.
      - name: results[].data.difficulty
        type: string
        description: The difficulty level of the trivia question.
      - name: total
        type: integer
        description: Total number of trivia results returned.
    errors:
      - code: validation_failed
        status: 422
        description: The filters array is missing, empty, or contains more than 50 items.
      - code: bad_request
        status: 400
        description: Invalid JSON, malformed request body, or unexpected field types.
      - code: unauthorized
        status: 401
        description: Missing API key.
      - code: forbidden
        status: 403
        description: Invalid or revoked API key.
    code_examples:
      curl: |
        curl -X POST "https://requiems.xyz/v1/entertainment/trivia/batch" \
          -H "requiems-api-key: YOUR_API_KEY" \
          -H "Content-Type: application/json" \
          -d '{
            "filters": [
              { "category": "sports", "difficulty": "easy" },
              { "category": "history", "difficulty": "medium" }
            ]
          }'
      python: |
        import requests

        url = "https://requiems.xyz/v1/entertainment/trivia/batch"

        headers = {
          "requiems-api-key": "YOUR_API_KEY",
          "Content-Type": "application/json"
        }

        payload = {
          "filters": [
            { "category": "sports", "difficulty": "easy" },
            { "category": "history", "difficulty": "medium" }
          ]
        }

        response = requests.post(url, headers=headers, json=payload)

        for item in response.json()["data"]["results"]:
          print(item["category"], "-", item["difficulty"])
          if item.get("error"):
            print("Error:", item["error"])
          else:
            print("Question:", item["data"]["question"])
            print("Options:", item["data"]["options"])
            print("Answer:", item["data"]["answer"])

      javascript: |
        const response = await fetch(
          'https://requiems.xyz/v1/entertainment/trivia/batch',
          {
            method: 'POST',
            headers: {
              'requiems-api-key': 'YOUR_API_KEY',
              'Content-Type': 'application/json'
            },
            body: JSON.stringify({
              filters: [
                { category: 'sports', difficulty: 'easy' },
                { category: 'history', difficulty: 'medium' }
              ]
            })
          }
        );

        const { data } = await response.json();

        data.results.forEach(item => {
          console.log(item.category, '-', item.difficulty);
          if (item.error) {
            console.log('Error:', item.error);
          } else {
            console.log('Question:', item.data.question);
            console.log('Options:', item.data.options);
            console.log('Answer:', item.data.answer);
          }
        });

      ruby: |
        require 'net/http'
        require 'json'

        uri = URI('https://requiems.xyz/v1/entertainment/trivia/batch')

        request = Net::HTTP::Post.new(uri)

        request['requiems-api-key'] = 'YOUR_API_KEY'
        request['Content-Type'] = 'application/json'

        request.body = {
          filters: [
            { category: 'sports', difficulty: 'easy' },
            { category: 'history', difficulty: 'medium' }
          ]
        }.to_json

        response = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) do |http|
          http.request(request)
        end

        JSON.parse(response.body)['data']['results'].each do |item|
          puts "#{item['category']} - #{item['difficulty']}"
          if item['error']
            puts "Error: #{item['error']}"
          else
            puts "Question: #{item['data']['question']}"
            puts "Options: #{item['data']['options']}"
            puts "Answer: #{item['data']['answer']}"
          end
        end
faq:
  - question: What categories are available?
    answer: >-
      Ten categories are supported: science, history, geography, sports, music, movies, literature, math, technology,
      and nature.
  - question: What difficulty levels are available?
    answer: >-
      Three levels are supported: easy, medium, and hard. When neither category nor difficulty is specified, a question
      is selected at random from the entire question pool.
  - question: Is the correct answer always one of the options?
    answer: Yes. The answer field always matches one of the values in the options array.
  - question: What happens if no questions match my filters?
    answer: >-
      The API returns a 404 not_found error. Try broadening your filter — for example, drop the difficulty filter or use
      a different category.
  - question: Are the answer options always in the same order?
    answer: >-
      No. While the options array is stable for a given question in the current dataset, the question returned is
      randomized per request.
performance:
  p50_ms: 946
  p95_ms: 1206
  p99_ms: 1232
  avg_ms: 989
  samples: 50
  measured_at: "2026-04-20"
